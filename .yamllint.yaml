extends: default

rules:
  line-length: disable

  truthy:
    allowed-values: ['true', 'false', 'on']
    level: warning

  document-start:
    present: false

  trailing-spaces:
    level: error

  empty-lines:
    max: 1

  new-line-at-end-of-file: disable
  new-lines: disable