APP_NAME=url-shortener-backend
APP_ENV=development
APP_VERSION=1.0.0
PORT=80

AWS_REGION=us-east-1
BASE_URL=https://api.yourdomain.xyz
# Only used as a local fallback when SSM is unavailable (e.g. local dev without AWS):
DYNAMODB_TABLE=url-shortener